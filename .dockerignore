# Dependencies (reinstalled inside the image via `npm ci`)
node_modules
npm-debug.log*

# Next.js build output
.next
out
build

# Git / CI
.git
.github
.gitignore

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Local env (compose supplies the needed vars; uncomment to bake in a file)
.env*
!.env.example

# OS / editor cruft
.DS_Store
*.pem
.vercel
coverage
*.tsbuildinfo
next-env.d.ts
